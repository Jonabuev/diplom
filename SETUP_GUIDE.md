# 🚀 Руководство по запуску проекта

## Предварительные требования

- Node.js 18+ 
- npm или yarn
- PostgreSQL (или другая SQL БД)

---

## ⚙️ Установка и настройка

### 1. Backend Setup

```bash
cd backend

# Установка зависимостей
yarn install
# или
npm install

# Создать файл .env
touch .env
```

**Пример `.env` для backend:**
```env
# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password
DATABASE_NAME=learning_platform

# JWT
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# Server
PORT=3001
```

**Запуск базы данных PostgreSQL:**
```bash
# Используя Docker (рекомендуется)
docker run --name learning-db \
  -e POSTGRES_PASSWORD=your_password \
  -e POSTGRES_DB=learning_platform \
  -p 5432:5432 \
  -d postgres:15
```

**Запуск backend:**
```bash
# Development режим
yarn start:dev
# или
npm run start:dev

# Production build
yarn build
yarn start:prod
```

Backend будет доступен на `http://localhost:3001`

Swagger документация: `http://localhost:3001/api`

---

### 2. Frontend Setup

```bash
cd frontend

# Установка зависимостей
yarn install
# или
npm install

# Создать файл .env.local
touch .env.local
```

**Пример `.env.local` для frontend:**
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

**Запуск frontend:**
```bash
# Development режим
yarn dev
# или
npm run dev

# Production build
yarn build
yarn start
```

Frontend будет доступен на `http://localhost:3000`

---

## 📝 Первоначальная настройка

### 1. Создание админ пользователя

Зарегистрируйтесь через UI или используйте API:

```bash
POST http://localhost:3001/api/auth/register
Content-Type: application/json

{
  "username": "admin",
  "email": "admin@example.com",
  "password": "admin123",
  "phone": "+79001234567"
}
```

Затем вручную в БД измените роль на `admin`:
```sql
UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';
```

### 2. Создание тестовых данных

Войдите под админом и через админ панель:
1. Перейдите в `/admin/books`
2. Создайте несколько книг
3. Добавьте главы к книгам
4. Сгенерируйте вопросы (пока это будут тестовые вопросы)

---

## 🔑 Роли пользователей

### Student (по умолчанию)
- Просмотр публичных книг
- Прохождение тестов
- Просмотр истории и достижений
- Добавление книг в свой список

### Author
- Всё что может Student
- Создание и редактирование своих книг
- Создание глав
- Управление вопросами

### Admin
- Всё что может Author
- Управление всеми книгами
- Управление всеми пользователями
- Доступ к полной статистике

---

## 📦 Структура проекта

```
diplom/
├── backend/
│   ├── src/
│   │   ├── auth/          # Аутентификация
│   │   ├── books/         # Управление книгами
│   │   ├── questions/     # Вопросы и генерация
│   │   ├── history/       # История ответов
│   │   ├── notifications/ # Уведомления
│   │   ├── users/         # Пользователи
│   │   └── entities/      # TypeORM entities
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── pages/         # Next.js страницы
│   │   │   ├── admin/     # Админ панель
│   │   │   ├── books/     # Книги
│   │   │   ├── quiz/      # Тесты
│   │   │   ├── achievements.tsx
│   │   │   ├── history.tsx
│   │   │   ├── profile.tsx
│   │   │   └── ...
│   │   └── lib/
│   │       └── api.ts     # API клиент
│   └── package.json
│
├── FEATURES.md            # Описание функционала
└── SETUP_GUIDE.md         # Это руководство
```

---

## 🧪 Тестирование

### Backend тесты
```bash
cd backend

# Unit tests
yarn test

# E2E tests
yarn test:e2e

# Test coverage
yarn test:cov
```

### Проверка API через Swagger
1. Запустите backend
2. Откройте `http://localhost:3001/api`
3. Используйте "Authorize" для JWT токена
4. Тестируйте endpoints

---

## 🐛 Troubleshooting

### Backend не запускается

**Проблема:** Ошибка подключения к БД
```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение:**
- Проверьте что PostgreSQL запущен
- Проверьте параметры в `.env`
- Убедитесь что БД создана

---

**Проблема:** Ошибки при миграции
```
Error: relation "users" does not exist
```

**Решение:**
- NestJS с TypeORM создаст таблицы автоматически при первом запуске
- Убедитесь что в `app.module.ts` есть `synchronize: true` (только для dev!)

---

### Frontend не запускается

**Проблема:** Cannot find module 'next'
```
Error: Cannot find module 'next'
```

**Решение:**
```bash
rm -rf node_modules
rm package-lock.json
npm install
```

---

**Проблема:** API недоступен
```
Error: Network Error
```

**Решение:**
- Проверьте что backend запущен на порту 3001
- Проверьте `NEXT_PUBLIC_API_URL` в `.env.local`
- Проверьте CORS настройки в `main.ts` (backend)

---

## 📱 Функционал по страницам

### Публичные страницы
- `/` - Главная
- `/login` - Вход
- `/register` - Регистрация
- `/books` - Каталог книг
- `/books/[id]` - Страница книги

### Защищенные страницы (требуется авторизация)
- `/profile` - Профиль пользователя
- `/history` - История прохождений
- `/achievements` - Достижения и уровни
- `/notifications` - Уведомления
- `/quiz/[bookId]/[chapterId]` - Прохождение теста

### Админ страницы (требуется роль admin/author)
- `/admin/books` - Управление книгами
- `/admin/chapters?bookId=...` - Управление главами

---

## 🔐 API Authentication

Все защищенные endpoints требуют JWT токен в заголовке:

```
Authorization: Bearer <your_jwt_token>
```

### Получение токена:
```bash
POST http://localhost:3001/api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

Ответ:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "uuid",
    "username": "user",
    "email": "user@example.com",
    "role": "student"
  }
}
```

---

## 🚀 Production Deployment

### Backend

```bash
cd backend

# Build
yarn build

# Start production
NODE_ENV=production yarn start:prod
```

**Важно для production:**
- Измените `JWT_SECRET` на криптостойкий ключ
- Отключите `synchronize: true` в TypeORM
- Используйте миграции для обновления схемы БД
- Настройте HTTPS
- Добавьте rate limiting
- Настройте логирование (Winston, Pino)

### Frontend

```bash
cd frontend

# Build
yarn build

# Start
yarn start
```

Или разверните на Vercel:
```bash
vercel deploy
```

---

## 📊 Мониторинг

### Логи Backend
```bash
# Development
yarn start:dev

# Логи сохраняются в консоль
```

### Метрики
- Количество пользователей
- Количество книг
- Количество пройденных тестов
- Популярные книги
- Средняя точность ответов

---

## 🔄 Обновление

### Backend
```bash
cd backend
git pull
yarn install
yarn build
yarn start:prod
```

### Frontend
```bash
cd frontend
git pull
yarn install
yarn build
yarn start
```

---

## 💾 Backup

### База данных
```bash
# Backup
pg_dump -U postgres learning_platform > backup.sql

# Restore
psql -U postgres learning_platform < backup.sql
```

### Файлы
Создайте резервные копии:
- Конфигурационные файлы (`.env`)
- Пользовательский контент (если есть file uploads)
- Логи

---

## 📚 Полезные команды

### Backend
```bash
# Генерация нового модуля
nest g module module-name

# Генерация контроллера
nest g controller controller-name

# Генерация сервиса
nest g service service-name

# Проверка кода
yarn lint
yarn format
```

### Frontend
```bash
# Создание новой страницы
touch src/pages/new-page.tsx

# Проверка типов
yarn type-check

# Lint
yarn lint
```

---

## 🎯 Следующие шаги

1. ✅ Запустите проект локально
2. ✅ Создайте тестовые данные
3. ✅ Протестируйте все функции
4. 🚀 Интегрируйте AI модель для генерации вопросов
5. 📱 Опционально: создайте мобильное приложение (React Native)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Проверьте сетевые запросы в DevTools
3. Убедитесь что все зависимости установлены
4. Проверьте версии Node.js и npm/yarn

---

**Удачи в разработке! 🚀**



